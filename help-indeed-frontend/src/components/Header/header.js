import "./styles.css"

export default function Header() {
    
    return (
        <div className="main-header">
            <div className="header">help indeed</div>
            {/* {props.loggedIn && <div>Account</div>} */}
        </div>
    );
}